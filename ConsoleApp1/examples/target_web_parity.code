integer ms = unixMilliseconds();
integer us = unixMicroseconds();
integer ns = monotonicNanoseconds();
integer ticks = monotonicTicks();
integer freq = monotonicTicksPerSecond();

print(ms > 0);
print(us >= ms * 1000);
print(ns >= 0);
print(ticks > 0);
print(freq > 0);
