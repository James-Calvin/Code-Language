print("host abi basic");

integer ms = unix_ms();
integer us = unix_us();
integer ns = mono_ns();
integer ticks = mono_ticks();
integer freq = mono_ticks_per_second();

print("unix_ms > 0: {ms > 0}");
print("unix_us > 0: {us > 0}");
print("mono_ns >= 0: {ns >= 0}");
print("mono_ticks > 0: {ticks > 0}");
print("mono_ticks_per_second > 0: {freq > 0}");
